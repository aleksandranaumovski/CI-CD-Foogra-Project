# Placeholder values — never real credentials. Patched in place by
# scripts/gen-secrets.sh for local clusters, or by the `deploy` job in
# .github/workflows/ci-cd.yml from repository secrets in CI.
apiVersion: v1
kind: Secret
metadata:
  name: db-secret
  namespace: foogra
type: Opaque
stringData:
  MYSQL_PASSWORD: "REPLACE_ME_mysql_password"
  MYSQL_ROOT_PASSWORD: "REPLACE_ME_mysql_root_password"
