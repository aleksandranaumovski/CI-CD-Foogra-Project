# Placeholder — patched in place by scripts/gen-secrets.sh (local cluster)
# or by the CI `deploy` job from the APP_KEY repository secret.
apiVersion: v1
kind: Secret
metadata:
  name: api-secret
  namespace: foogra
type: Opaque
stringData:
  APP_KEY: "REPLACE_ME_app_key"
